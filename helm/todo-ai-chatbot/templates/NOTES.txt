Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{- if .Values.ingress.enabled }}
APPLICATION ACCESS:
-------------------
You can access the application by visiting: http://{{ .Values.ingress.hostname }}

{{- else }}
SERVICE ACCESS:
---------------
Get the frontend service URL by running:
  export FRONTEND_SERVICE_IP=$(kubectl get svc {{ include "todo-ai-chatbot.fullname" . }}-frontend -n {{ .Release.Namespace }} -o jsonpath='{.spec.clusterIP}')
  echo "Frontend URL: http://$FRONTEND_SERVICE_IP:{{ .Values.frontend.service.port }}"

Or use port forwarding:
  kubectl port-forward svc/{{ include "todo-ai-chatbot.fullname" . }}-frontend {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }} -n {{ .Release.Namespace }}
  echo "Visit: http://localhost:{{ .Values.frontend.service.port }}"
{{- end }}

AI DEVOPS TOOLS:
----------------
- Use kubectl-ai to interact with your deployments: kubectl-ai describe pods
- Use kagent for health checks and diagnostics
- Use Docker AI (Gordon) for container optimization

CONFIGURATION:
--------------
- Frontend replicas: {{ .Values.frontend.replicaCount }}
- Backend replicas: {{ .Values.backend.replicaCount }}
- MCP replicas: {{ .Values.mcp.replicaCount }}

NEXT STEPS:
-----------
1. Verify all pods are running: kubectl get pods
2. Check service endpoints: kubectl get svc
3. Access the application via the ingress or service
4. Test the AI chatbot functionality
5. Scale components as needed: kubectl scale deployment <deployment-name> --replicas=N